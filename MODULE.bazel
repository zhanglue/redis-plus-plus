"""
This module defines the Bazel entry point for redis-plus-plus.
"""

module(
    name = "redis_plus_plus",
    version = "1.3.15",
    repo_name = "redis_plus_plus",
)

bazel_dep(name = "rules_cc", version = "0.2.14")
bazel_dep(name = "hiredis", version = "1.3.0")
